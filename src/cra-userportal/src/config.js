const config = {
    // This config setting is used to determine if the display field
    // should be shown on the create form for SOW and Invoice.
    // Setting to 'false' will hide the display of all fields on the
    // create screens for SOW and Invoice, with the exception of the
    // file upload and Vendor fields.
    displayFieldOnCreate: true
    // Before integrating AI into the app, this setting should be set to 'true'
    // After integrating AI into the app, this setting should be set to 'false'
};
export default config;
